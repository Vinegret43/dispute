import { View } from "styles/retro/main.slint";
import { GlobalState } from "globals.slint";
import "fonts/editundo.ttf";

export * from "globals.slint";

export component AppWindow inherits Window {
    title: "Dispute";
    icon: @image-url("../../assets/dispute.png");
    default-font-family: "Edit undo BRK";
    min-width: 408px;
    min-height: 600px;
    View {}
}
